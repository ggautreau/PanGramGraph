# sourced by every job: the project space, its environment, and the Hugging Face cache (the home is full)
export PGG_ROOT=/mnt/beegfs/projects/pangenome_models/pangramgraph
export HF_HOME=$PGG_ROOT/hf PATH=$PGG_ROOT/env/bin:$PATH PYTHONUNBUFFERED=1 PYTHONWARNINGS=ignore
cd $PGG_ROOT/code
